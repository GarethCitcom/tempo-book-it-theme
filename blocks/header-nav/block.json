{
	"$schema": "https://schemas.wp.org/trunk/block.json",
	"apiVersion": 3,
	"name": "tempo/header-nav",
	"title": "Portal header nav",
	"category": "theme",
	"description": "Role-aware pinned header navigation: basket pill (customers, plugin-rendered) plus the Book classes / My classes link. Assigning a menu to the viewer's header location drops the link, but the basket pill — and its checkout countdown — always stays.",
	"textdomain": "tempo-book-it-theme",
	"supports": {
		"html": false,
		"multiple": false,
		"reusable": false
	},
	"render": "file:./render.php"
}
